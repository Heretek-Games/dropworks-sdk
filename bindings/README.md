# Language bindings

Each binding wraps the Dropworks HTTP/WebSocket contract described in
`../src/index.ts`.

- `c/` — C (Windows, Linux, macOS)
- `rust/` — Rust crate
- `csharp/` — C# / Unity
- `gdscript/` — Godot 4 GDScript
